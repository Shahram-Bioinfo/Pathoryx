"""
pathoryx_enterprise.runtime — platform runtime utilities.

Provides one-call Windows DLL registration helpers that must be invoked
BEFORE any openslide import, regardless of which service is starting.
"""
